#include<bits/stdc++.h>
using namespace std;

#define ll long long

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	int a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << 2*(a+b)*c << endl << 2*(a+b)*c*d;
}
